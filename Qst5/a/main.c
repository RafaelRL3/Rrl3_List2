#include <stdio.h>

typedef long long int ll;

extern void test(ll *i, ll *i2);

int main(void)
{
    ll i = 12;
    printf("Qtd de vezes que vai ser feita o loop, %lld\n", i);
    test(&i, %i2);
    return 0;
}